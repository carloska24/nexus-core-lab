# Example only. Replace locally; never commit your real .env.
POSTGRES_PASSWORD=change_me
POSTGRES_PORT=5433
# Export DATABASE_URL separately for Go; Go does not load .env automatically.
DATABASE_URL=postgres://nexus:change_me@localhost:5433/nexus_core_lab?sslmode=disable
